## Packages
framer-motion | Page transitions and interactive animations

## Notes
Mobile-first app wrapper restricts max-width to simulate a native app on desktop.
Zod schemas imported from @shared/schema and @shared/routes.
API expects requests matching the predefined routes and params.
